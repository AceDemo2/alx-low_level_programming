#include "main.h"

/**
 * _print_rev_recursion - prints a string in reverse
 * @s: string to print
 */
void _print_rev_recursion(char *s)
{
	int i = 0;

	if (i < 0)
		return;
	else
	_putchar(_print_rev_recursion(&s[i++]);
}
